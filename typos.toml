[files]
extend-exclude = ["vendor/h3"]

[default.extend-words]
# QUIC packet number abbreviation.
pn = "pn"
# libc msghdr field name.
controllen = "controllen"
# Common HTTP benchmark tool.
wrk = "wrk"
# Windows SDDL SID abbreviation (Built-in Administrators).
ba = "ba"
